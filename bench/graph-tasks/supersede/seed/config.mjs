// The project's runtime config. Every setting's value EVOLVED over the project's
// history — decided, then revised one or more times in later commits (the reasons live
// in the commit bodies, never in this file). Implement `config()` to return the CURRENT
// value of every setting: the LATEST revision wins, not the first.
export function config() {
  // A later step implements this from the project's decision history.
  return {};
}
